const AddTradeProvider = ({alertStatus}) => {
    const {state: {alert, projects }, dispatch} = useContext(appContext)
    console.log(projects)
        
    
           
        return (
            <>
               
            </>
        )
    }
    
    export default AddTradeProvider
    